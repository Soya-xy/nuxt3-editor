import { IComponents } from "../types"

export const V3 = (item?: IComponents) => {
  console.log("🚀 ~ file: page.ts:4 ~ V3 ~ item:", item)
  return `<script setup>
</script>

<template>
  <div>
    index
  </div>
</template>
`
}

